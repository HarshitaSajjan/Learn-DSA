#include<bits/stdc++.h>
using namespace std;

//Method1 (sorting) - O(nlogn) time sc O(1) or O(n)

//Method2(heap)
/*since top is smaller than sum pop it, if it is still smaller then sum and pop until top is not smaller than sum, return res

TC n+ res*logn  
SC n or n or 1
*/
